# Build output
target/
*.jar.original

# Local H2 database (runtime data — never commit)
data/

# Logs
*.log

# OS / editor noise
.DS_Store

# Customer requirement / patent source documents — kept local, NOT published to the remote
zakazdar/
