package com.ideaqr.gateway.domain.enums;

/** The context a user session is operating in. */
public enum SessionMode {
    /** Default personal context. */
    PERSONAL,
    /** Working context: organization + work role active. */
    WORKING
}
